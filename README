NetCore-Ts-Converter

To use this converter: 

Build your API

Open powershell and enter the following:

1. Set-Location "C:\Repos\Swagger-Ts-Converter\Swagger-Ts-Converter.PowerShell.Main\APIConverter"

2. Import-Module .\bin\Debug\netstandard2.0\APIConverter.dll

3. Get-Command -Module APIConverter

4. ConvertTo-TypeScriptFromOpenAPI -SFP "D:\PATH\TO\YOUR\SOLUTION"

This command looks for a swagger file in the directory specified and outputs api-types.ts to the same folder.

or 

4. ConvertToTS_LocalHost -Port 8080

This command will read the swagger file hosted locally and output api-types.ts to the desktop
